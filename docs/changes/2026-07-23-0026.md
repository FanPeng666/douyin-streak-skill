# 2026-07-23-0026

## 为什么修改

1. 项目记忆文件 `MEMORY.md` 放在 `.workbuddy/memory/` 中不方便用户直接查看和编辑，应移到项目根目录下
2. 日志时间格式需要统一规范，支持精确到分钟的时间戳
3. 需要建立自动变更记录机制，让 AI 在每次修改后自动维护 CHANGELOG

## 修改了哪些文件

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `memory/MEMORY.md` | 新建 | 从 `.workbuddy/memory/` 移到项目根目录 `memory/MEMORY.md` |
| `scripts/utils.js` | 新增函数 | 添加 `fmtLogTime()` 和 `validLogTime()` 时间格式工具 |
| `scripts/auto_streak.js` | 更新 | 报告时间改用 `fmtLogTime()` |
| `CHANGELOG.md` | 重写 | 改为概要索引格式，日期改为 YYYY-MM-DD-HHmm |
| `prompts/自动续火花.md` | 新增章节 | 第十二章「任务记录」，规范自动变更记录 |
| `memory/MEMORY.md` | 补充 | 新增「项目工作约定」，规范自动维护 |
| `docs/changes/2026-07-23-0021.md` | 新建 | 详细变更记录模板 |

## Prompt 变化

- 新增「十二、任务记录」章节，规定执行完成后自动写变更记录
- 时间格式统一为 `YYYY-MM-DD-HHmm`

## Skill 变化

- 无（SKILL.md 未改）

## 注意事项

- `fmtLogTime()` 输出不带冒号的紧凑格式（`1430` 而不是 `14:30`）
- `validLogTime()` 支持语义校验：月份 01-12、日期 01-31、小时 00-23、分钟 00-59
- `docs/changes/` 文件名命名规则：`YYYY-MM-DD-HHmm.md`
